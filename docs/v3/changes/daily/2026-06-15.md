# 2026-06-15

## Scope

- Added Phase 5.6 dashboard read-model and API consumer guide documentation.
- Added bounded JSON examples for the current template coverage and profile-driven task creation API shape.
- Strengthened Control Plane coverage export contract tests without changing runtime API behavior.
- Updated docs/v3 indexes to use the current-day daily change log.
- Added Phase 5.7 planning contracts for duration-based execution, stop policy, distributed agent shards, dataset partitioning, and distributed result aggregation.
- Updated dashboard, external API, consumer guide, distributed deployment, and roadmap docs with future `execution`, `distribution`, `dataset`, `shards`, and `result_aggregation` contract objects.
- Implemented Phase 5.8 single-agent duration-based execution MVP as an additive preview API/runtime feature.
- Added `execution` validation and normalization into existing `LoadTestTask.parameters["execution"]` without a database migration.
- Passed execution metadata through worker k6 environment variables, JMeter `-J...` properties, and subprocess timeout handling.
- Updated representative k6 and JMeter assets only; distributed shards, dataset partition runtime, dashboard UI, token-system changes, and scheduler behavior remain future work.
- Added a root-level `CODE_OF_CONDUCT.md` and linked it from the public docs indexes.
- Implemented Phase 5.9 manual shard distribution metadata MVP.
- Added `distribution` validation and generated `parameters["shard_execution_plan"]` without a database migration.
- Added `GET /api/tasks/{id}/shard-plan/` for read-only shard execution plan export.
- Passed one shard dataset assignment to k6 environment variables and JMeter `-J...` properties when worker payloads include shard metadata.
- Added Core external client contract and API compatibility planning documents.
- Defined stable candidate, experimental runtime, and planning-only API contract classifications.
- Added Core boundary guidance for external clients, downstream integrations, compatible dashboards, and public project governance.
- Implemented Core Phase 7 artifact lifecycle and result reporting contract hardening.
- Added derived artifact metadata rows, a structured artifact download placeholder route, result provenance fields, retention guidance, and percentile merge limitations without adding a dashboard or durable artifact store.
- Implemented Core Phase 8 persisted artifact manifest MVP.
- Added a minimal `TaskArtifact` model, controlled object reference validation, manifest registration helper, persisted-over-derived artifact merge, and download-route existence checks without adding real file download or a report center.
- Implemented Core Phase 9 worker artifact registration MVP.
- Added worker-built artifact manifest entries to the existing result callback flow, persisted manifest registration on result create, and evidence-based k6/JMeter worker artifact mapping without exposing local filesystem paths.
- Implemented Core Phase 10 artifact manifest versioning and checksum/size enrichment.
- Added versioned worker artifact manifest envelope support, SHA-256 validation, backward-compatible legacy list payload support, and public artifact contract version metadata without adding download or object storage behavior.

## Changed Files

- `docs/v3/README.md`
- `docs/v3/specs/README.md`
- `docs/v3/specs/external-client-contract.md`
- `docs/v3/specs/api-versioning-policy.md`
- `docs/v3/specs/artifact-lifecycle.md`
- `control-plane/apps/tasks/artifact_contract.py`
- `control-plane/apps/tasks/artifact_registry.py`
- `docs/v3/specs/dashboard-read-model.md`
- `docs/v3/specs/api-consumer-guide.md`
- `docs/v3/specs/examples/templates-coverage-response.json`
- `docs/v3/specs/examples/task-template-profile.json`
- `docs/v3/specs/examples/create-task-from-profile.json`
- `docs/v3/specs/external-api-v1.md`
- `docs/v3/specs/task-execution-model.md`
- `docs/v3/specs/distributed-agent-execution.md`
- `docs/v3/domains/p-loadtesting-web-dashboard.md`
- `docs/v3/domains/p-loadtesting-core-boundary.md`
- `docs/v3/runbooks/distributed-deployment.md`
- `docs/v3/roadmap/github-issues-dashboard-api-deployment.md`
- `control-plane/apps/tasks/execution.py`
- `control-plane/apps/tasks/distribution.py`
- `control-plane/apps/tasks/read_models.py`
- `control-plane/apps/tasks/tests.py`
- `control-plane/apps/tasks/serializers.py`
- `control-plane/apps/tasks/template_registry.py`
- `control-plane/apps/tasks/urls.py`
- `control-plane/apps/tasks/views.py`
- `control-plane/apps/tasks/models.py`
- `control-plane/apps/tasks/migrations/0003_taskartifact.py`
- `workers/agent.py`
- `workers/artifacts.py`
- `workers/tests/test_agent_execution.py`
- `control-plane/apps/results/serializers.py`
- `control-plane/apps/results/views.py`
- `control-plane/apps/tasks/artifact_contract.py`
- `control-plane/apps/tasks/read_models.py`
- `docs/v3/specs/api-versioning-policy.md`
- `engines/k6/lib/execution.js`
- `engines/k6/README.md`
- `engines/k6/target_apps_payload_download.js`
- `engines/k6/target_apps_echo_smoke.js`
- `engines/k6/target_apps_latency_delay.js`
- `engines/k6/target_apps_auth_checkout.js`
- `engines/jmeter/README.md`
- `engines/jmeter/target_apps_echo_latency_plan.jmx`
- `engines/jmeter/target_apps_payload_crud_plan.jmx`
- `engines/jmeter/target_apps_auth_flow_plan.jmx`
- `target-apps/task-templates/echo-api.yaml`
- `target-apps/task-templates/latency-api.yaml`
- `target-apps/task-templates/payload-api.yaml`
- `target-apps/task-templates/auth-flow-api.yaml`
- `CODE_OF_CONDUCT.md`
- `README.md`
- `docs/v3/README.md`
- `docs/v3/changes/daily/2026-06-15.md`

## Summary

- Defined the future dashboard read model over existing preview endpoints, with the Phase 6 MVP limited to Target Catalog, Profile Catalog, Coverage Matrix, and Create Task Wizard.
- Documented current API consumer usage for template listing, coverage export, and manifest-driven task creation.
- Added example JSON payloads using existing target profiles and placeholder-only access examples.
- Extended Control Plane tests to assert coverage export summary counts, required profile fields, resolvable exact equivalents, and non-empty coverage groups.
- Kept target apps, DB schema, worker behavior, token implementation, and dashboard implementation unchanged.
- Defined the future `execution` object with duration, ramp-up, ramp-down, stop policy, graceful stop, max run, iteration limit, and data policy fields.
- Defined the future distributed execution model around logical tasks, task runs, shards, agent selectors, agent labels, dataset shards, result shards, artifact collection, retry, partial success, and cancellation.
- Documented dataset partition strategies, with `manual_ranges`, `range`, and `manual` as MVP candidates and `hash`, `weighted`, and `round_robin` as future extensions.
- Documented distributed aggregation rules: sum request counts, recalculate error rate and throughput, avoid averaging latency percentiles, and require mergeable data for global p95/p99.
- Kept Phase 5.7 as planning and contract design only; no worker runtime, database schema, target app, or complete scheduler implementation was added.
- Added request/template/engine-default precedence for task `execution` metadata.
- Added current engine defaults for k6 and JMeter duration, ramp, grace, timeout, iteration, stop policy, and data policy.
- Added validation for supported `stop_policy=graceful_stop|hard_stop` and `data_policy=duration_first|iteration_first`, with future-policy rejection messages for advanced stop policies.
- Added profile-level execution defaults to `echo-api`, `latency-api`, `payload-api`, and `auth-flow-api` representative profiles.
- Added worker timeout diagnostics that post a failed result with stop metadata under the existing task/result states.
- Updated docs from Phase 5.7 planning-only wording to Phase 5.8 single-agent MVP wording while keeping distributed execution and dataset partition runtime as future work.
- Added a project Code of Conduct adapted from Contributor Covenant 2.1 and exposed it in the root README and docs trunk index.
- Added manual shard distribution metadata support for `mode=manual_shards` and `result_merge_policy=summary_only`.
- Added dataset partition validation for safe sources, allowed formats, non-negative offsets, and positive limits.
- Added shard execution plan metadata with per-shard agent selector labels, dataset partition, execution object, and summary-only aggregation contract.
- Kept the existing scheduler as one task to one worker; no shard table, worker claim runtime, retry lifecycle, dashboard UI, token changes, target app changes, or exact percentile merge was added.
- Defined the Core contract for external clients to integrate through documented HTTP APIs, JSON payloads, and public schemas.
- Classified template registry and coverage metadata as stable candidates, `execution` and `distribution` as experimental runtime contracts, and token API, advanced scheduling, advanced aggregation, and artifact browser APIs as planning-only.
- Documented Core anti-coupling rules so downstream integration requests must be expressed as generic Core capabilities.
- Linked the new contract, versioning policy, and Core boundary documents from the active docs/v3 indexes and root public README.
- Implemented Core Phase 6 task history and result read contracts.
- Replaced `GET /api/tasks/` list output with a `source`, `summary`, and `items` read-model envelope.
- Stabilized `GET /api/tasks/{id}/` detail output with task, parameter summary, execution, distribution, result status, and warnings.
- Added `GET /api/tasks/{id}/result-summary/` with available mapping from `TestResult` or `not_available` placeholder responses.
- Added `GET /api/tasks/{id}/artifacts/` as an artifact metadata placeholder without download or report generation.
- Kept DB schema, target apps, token system, scheduler, worker runtime, advanced aggregation, and artifact download unchanged.
- Hardened `GET /api/tasks/{id}/result-summary/` with stable provenance metadata and explicit engine-reported percentile warnings.
- Hardened `GET /api/tasks/{id}/artifacts/` with engine-derived artifact rows, truthful `planned` / `available` / `missing` states, summary counts, and no local path exposure.
- Added `GET /api/tasks/{id}/artifacts/{artifact_id}/download/` as a structured `501 not implemented` preview placeholder route.
- Added `docs/v3/specs/artifact-lifecycle.md` as the canonical spec for artifact metadata, retention policy, download boundaries, security notes, and non-goals.
- Kept complete report generation, artifact browser UI, unsafe filesystem download, token system changes, scheduler redesign, and exact percentile merge out of scope.
- Added a persisted `TaskArtifact` manifest MVP with safe metadata fields, controlled object references, and `(task, artifact_id)` upsert behavior.
- Updated `GET /api/tasks/{id}/artifacts/` to merge persisted manifest rows with derived fallback metadata and to expose `planned_count`, `expired_count`, and `external_count`.
- Kept `GET /api/tasks/{id}/artifacts/{artifact_id}/download/` as a structured placeholder while adding `404` for unknown artifact ids.
- Added worker-built `artifact_manifest` payload support to `POST /api/tasks/{id}/results/` so the worker can register persisted artifact metadata through the existing callback path.
- Added deterministic worker artifact mappings for k6, JMeter, and unknown engines with `available` state gated by real evidence only.
- Kept object references logical and task-scoped, rejected unsafe local path inputs, and kept the download route as a structured placeholder.
- Added `artifact_manifest_version: "1.0"` as the current worker/public artifact manifest contract version.
- Added backward-compatible support for legacy list-only worker artifact payloads while preferring the versioned envelope shape.
- Added safe `size_bytes` and `checksum_sha256` enrichment from in-memory evidence such as captured `stdout`, captured `stderr`, and persisted `raw_report`.
- Added SHA-256 format validation and public artifact response `contract.artifact_manifest_version` metadata.

## Validation

- `git diff --check` passed.
- JSON examples parse with `./.venv/bin/python -m json.tool`.
- Restricted wording scan passed with no matches for the requested phrases.
- `./.venv/bin/python -m pytest target-app/ target-apps/tests -v` passed.
- `cd control-plane && ./.venv/bin/python manage.py check` passed.
- `cd control-plane && ./.venv/bin/python manage.py test apps/ --verbosity=2` passed.
- `docker compose -f target-apps/docker-compose.target-apps.yml config --quiet` passed.
- `bash target-apps/scripts/smoke_docker_target_apps.sh` passed.
- Phase 5.7 `git diff --check` passed.
- Phase 5.7 restricted wording scan over changed planning docs passed.
- Phase 5.7 `./.venv/bin/python -m pytest target-app/ target-apps/tests -v` passed.
- Phase 5.7 `control-plane/.venv/bin/python manage.py check` equivalent was run from `control-plane/` as `./.venv/bin/python manage.py check` and passed.
- Phase 5.7 `control-plane/.venv/bin/python manage.py test apps/ --verbosity=2` equivalent was run from `control-plane/` as `./.venv/bin/python manage.py test apps/ --verbosity=2` and passed.
- Phase 5.7 `docker compose -f target-apps/docker-compose.target-apps.yml config --quiet` passed.
- Phase 5.7 `bash target-apps/scripts/smoke_docker_target_apps.sh` passed.
- Phase 5.8 targeted `./.venv/bin/python -m pytest workers/tests -v` passed.
- Phase 5.8 targeted `cd control-plane && ./.venv/bin/python manage.py test apps.tasks --verbosity=2` passed.
- `git diff --check` should pass after the Code of Conduct addition.
- Root README and `docs/v3/README.md` both link the new conduct policy.
- Phase 5.8 `git diff --check` passed.
- Phase 5.8 `./.venv/bin/python -m pytest target-app/ target-apps/tests -v` passed.
- Phase 5.8 `cd control-plane && ./.venv/bin/python manage.py check` passed.
- Phase 5.8 `cd control-plane && ./.venv/bin/python manage.py test apps/ --verbosity=2` passed.
- Phase 5.8 `./.venv/bin/python -m pytest workers/tests -v` passed.
- Phase 5.8 `docker compose -f target-apps/docker-compose.target-apps.yml config --quiet` passed.
- Phase 5.8 `bash target-apps/scripts/smoke_docker_target_apps.sh` passed.
- Phase 5.8 restricted wording scan over changed docs and planned commit message passed.
- Phase 5.9 targeted `cd control-plane && ./.venv/bin/python manage.py test apps.tasks --verbosity=2` passed.
- Phase 5.9 targeted `./.venv/bin/python -m pytest workers/tests -v` passed.
- Phase 5.9 `git diff --check` passed.
- Phase 5.9 `./.venv/bin/python -m pytest target-app/ target-apps/tests -v` passed.
- Phase 5.9 `cd control-plane && ./.venv/bin/python manage.py check` passed.
- Phase 5.9 `cd control-plane && ./.venv/bin/python manage.py test apps/ --verbosity=2` passed.
- Phase 5.9 `./.venv/bin/python -m pytest workers/tests -v` passed.
- Phase 5.9 `docker compose -f target-apps/docker-compose.target-apps.yml config --quiet` passed.
- Phase 5.9 `bash target-apps/scripts/smoke_docker_target_apps.sh` passed.
- Phase 5.9 restricted wording scan over added doc lines and planned commit message passed.
- Phase 9 `./.venv/bin/python -m pytest workers/tests -v` should verify worker artifact registration helper coverage.
- Phase 9 `cd control-plane && ./.venv/bin/python manage.py test apps/ --verbosity=2` should verify result callback manifest registration and artifact read-model coverage.
- Phase 10 should verify versioned and legacy artifact manifest registration paths plus checksum/size read-model coverage.

## Follow-Ups

- Build the Phase 6 dashboard MVP only after the read model is accepted.
- Keep scoped-token implementation separate from dashboard read-model documentation.
- Expand duration helper coverage to remaining k6 and JMeter assets after representative MVP behavior is accepted.
- Implement full distributed shard runtime, worker claim, retry lifecycle, and exact percentile merge only after manual shard metadata is stable.
- Add durable artifact storage and controlled download only after retention policy, cleanup policy, and external storage contracts are accepted.
- Wire future worker output registration through the manifest helper only after artifact object reference policy and lifecycle cleanup rules are stable.
- Add a future internal artifact callback or storage-backed object reference flow only after the current result-callback-based registration path is accepted.
- Introduce later artifact manifest versions only with an explicit compatibility and migration note in `docs/v3/specs/api-versioning-policy.md`.

## Commit

- Commit: Phase 5.9 implementation is recorded in repository history after this daily entry update.
