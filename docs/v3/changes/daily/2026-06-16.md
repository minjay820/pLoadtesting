# 2026-06-16

## Core Phase 20A local smoke image builds

## Scope

- Prepared local smoke image build flow for compatible external client deployment validation.
- Built local release candidate images for control plane, worker, and target apps.
- Kept scope limited to image build hygiene, runbook documentation, and validation.

## Changed Files

- `control-plane/.dockerignore`
- `workers/.dockerignore`
- `target-apps/.dockerignore`
- `docs/v3/runbooks/local-smoke-image-build.md`
- `docs/v3/runbooks/README.md`
- `docs/v3/README.md`
- `README.md`
- `docs/v3/changes/daily/2026-06-16.md`

## Summary

- Confirmed existing Dockerfiles are present for `control-plane/`, `workers/`, and `target-apps/`.
- Added minimal `.dockerignore` files to keep live env files, Python cache, local venvs, and generated artifacts out of build contexts.
- Built `local/ploadtesting-control-plane:0.1.0-rc.1`.
- Built `local/ploadtesting-worker:0.1.0-rc.1`.
- Built `local/ploadtesting-target-apps:0.1.0-rc.1`.
- Recorded image IDs, local repo digests, creation times, sizes, and build commands in the local smoke image build runbook.
- Re-verified the local smoke image build record against fresh `docker build` and `docker image inspect` output.
- Did not push registry images.
- Did not add runtime features, artifact download, report viewer, scheduler behavior, token system, database migration, or target app features.

## Validation

- `docker build -t local/ploadtesting-control-plane:0.1.0-rc.1 control-plane`
- `docker build -t local/ploadtesting-worker:0.1.0-rc.1 workers`
- `docker build -t local/ploadtesting-target-apps:0.1.0-rc.1 target-apps`
- `docker image inspect local/ploadtesting-control-plane:0.1.0-rc.1`
- `docker image inspect local/ploadtesting-worker:0.1.0-rc.1`
- `docker image inspect local/ploadtesting-target-apps:0.1.0-rc.1`
- `git diff --check`
- `./.venv/bin/python -m pytest target-app/ target-apps/tests -v`
- `cd control-plane && ./.venv/bin/python manage.py check`
- `cd control-plane && ./.venv/bin/python manage.py test apps/ --verbosity=2`
- `./.venv/bin/python -m pytest workers/tests -v`
- `docker compose -f target-apps/docker-compose.target-apps.yml config --quiet`
- `bash target-apps/scripts/smoke_docker_target_apps.sh`

## Follow-Ups

- Use the local image set for downstream runtime smoke only after the consuming deployment pack is configured for local image mode.
- Registry publishing remains a separate release step.

## Commit

- Commit: Phase 20A local smoke image build documentation and verification is recorded in repository history after this daily entry update.
