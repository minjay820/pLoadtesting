# 2026-06-20 Daily Change Log

## Core Phase 21M worker registration and demo task execution smoke

## Scope

- Added configurable Worker Agent registration metadata for deployment smoke.
- Documented compatible idle worker requirements for controlled demo task dispatch.
- Added protected running lifecycle callback support through the existing result callback route.
- Kept controlled demo task submit limited to safe demo profiles.
- Kept unauthenticated external result callback, artifact download, unsafe submit, target override submit, and arbitrary task operation protected or blocked.
- Did not add target apps, DB migrations, artifact download, report viewer, formal access system, or image startup migration.

## Changed Files

- `workers/agent.py`
- `workers/tests/test_agent_execution.py`
- `docker-compose.yml`
- `control-plane/apps/results/views.py`
- `control-plane/apps/tasks/tests.py`
- `docs/v3/README.md`
- `docs/v3/specs/external-api-v1.md`
- `docs/v3/specs/external-client-contract.md`
- `docs/v3/runbooks/external-db-runtime.md`
- `docs/v3/runbooks/local-smoke-image-build.md`
- `docs/v3/changes/daily/2026-06-20.md`

## Summary

- No-compatible-worker root cause: Core dispatch selects from the persisted worker registry, not process smoke output. A worker must register and heartbeat into the same Control Plane runtime with `status=online`, `active_task_count=0`, and a matching engine capability before Core can dispatch a safe demo task.
- Deployment root cause confirmed in bounded integration smoke: worker registration can be rejected with HTTP 400 when Core `DJANGO_ALLOWED_HOSTS` does not include the host name used in `CONTROL_PLANE_URL`.
- Worker registration now supports `WORKER_PORT`, `WORKER_CAPABILITIES`, and optional `WORKER_ADVERTISE_IP` in addition to `CONTROL_PLANE_URL`, `WORKER_NAME`, and `PLOADTESTING_API_TOKEN`.
- The default compose worker service now declares `WORKER_PORT=8100` and `WORKER_CAPABILITIES=k6,jmeter`.
- Safe demo task dispatch still moves to `dispatched` only after worker HTTP delivery succeeds; no worker keeps the task `pending` with a diagnostic.
- The Worker Agent now posts `execution_status=running` through the protected result callback route when execution starts.
- Core accepts the protected running callback without creating a `TestResult`, sets task `status=running`, and fills `started_at`.
- Final protected callbacks still persist results and move tasks to `completed` or `failed`.

## Validation

- `git diff --check`
- `./.venv/bin/python -m pytest target-app/ target-apps/tests -v`
- `cd control-plane && ./.venv/bin/python manage.py check`
- `cd control-plane && ./.venv/bin/python manage.py test apps.tasks --verbosity=2`
- `cd control-plane && ./.venv/bin/python manage.py test apps/ --verbosity=2`
- `cd control-plane && PLOADTESTING_ENABLE_DEMO_TASK_API=true ./.venv/bin/python manage.py test apps/ --verbosity=2`
- `./.venv/bin/python -m pytest workers/tests -v`
- `docker compose -f target-apps/docker-compose.target-apps.yml config --quiet`
- `bash target-apps/scripts/smoke_docker_target_apps.sh`
- `docker build -t local/ploadtesting-control-plane:0.1.0-rc.1 control-plane`
- `docker image inspect local/ploadtesting-control-plane:0.1.0-rc.1 --format '{{.Id}} {{.Created}} {{.Size}} {{json .Config.Cmd}}'`
- `docker build -t local/ploadtesting-worker:0.1.0-rc.1 workers`
- `docker image inspect local/ploadtesting-worker:0.1.0-rc.1 --format '{{.Id}} {{.Created}} {{.Size}} {{json .Config.Cmd}}'`

Image result:

- Control Plane image ID: `sha256:6ef74008d955a29cb96f4c4290835fe3f3c58b8c07e58044d22b3c12a411068b`
- Control Plane created: `2026-06-19T17:10:11.072830216Z`
- Control Plane size: `79482616`
- Control Plane CMD: `["python","manage.py","runserver","0.0.0.0:8000"]`
- Worker image ID: `sha256:c7a35121837af7a4bdf0e6c2ece84dcb5d273415c48595aac2e98e8ab3ff3cb9`
- Worker created: `2026-06-19T17:01:31.010777086Z`
- Worker size: `335274457`
- Worker CMD: `["python","agent.py"]`

Bounded integration smoke:

- Catalog and coverage endpoints: HTTP 200.
- Safe demo submit with `echo-api` / `echo-k6-smoke`: HTTP 201, initial status `dispatched`, worker assigned.
- Observed task status progression: `dispatched -> running -> failed`.
- The final failed state had a diagnostic because the bounded smoke did not start the target service in the same Docker network.
- Shard-plan: HTTP 200, `mode=single`, `status=not_applicable`.
- Result summary: HTTP 200, `status=available`.
- Artifact metadata: HTTP 200, 5 rows.
- Artifact download: HTTP 403.
- Unauthenticated external result callback: HTTP 403.

## Follow-Ups

- Run external deployment smoke with Core, Worker, and the safe local target service sharing the intended deployment network before release candidate readiness review.

## Core Phase 21O worker safe demo script and advertised address fix

## Scope

- Packaged safe demo engine assets into the Worker image build context.
- Kept the safe demo catalog profile IDs and script paths stable.
- Updated the safe demo target URL to the compose-local `echo-api` service address for Worker-container execution.
- Added a named target-apps compose network knob so deployment compose files can declare network sharing instead of relying on manual network attachment.
- Documented the advertised address strategy for single-host compose and split-host deployment.
- Kept task operation constraints, unauthenticated result callback protection, and artifact download protection unchanged.
- Did not add target apps, DB migrations, artifact download, report viewer, scheduler, formal access system, or image startup migration.

## Changed Files

- `workers/engines/k6/target_apps_echo_smoke.js`
- `workers/engines/k6/lib/execution.js`
- `workers/engines/jmeter/target_apps_echo_latency_plan.jmx`
- `workers/tests/test_agent_execution.py`
- `control-plane/apps/tasks/catalog/task-templates/echo-api.yaml`
- `control-plane/apps/tasks/tests.py`
- `target-apps/task-templates/echo-api.yaml`
- `target-apps/docker-compose.target-apps.yml`
- `target-apps/scripts/smoke_docker_target_apps.sh`
- `target-apps/tests/test_suite.py`
- `docs/v3/specs/external-api-v1.md`
- `docs/v3/specs/external-client-contract.md`
- `docs/v3/runbooks/external-db-runtime.md`
- `docs/v3/runbooks/local-smoke-image-build.md`
- `docs/v3/changes/daily/2026-06-20.md`

## Summary

- Missing script root cause: the Worker image is built from the `workers/` Docker context, while the safe demo catalog script paths point to repository-relative `engines/...` files. Source compose worked through a bind mount, but the standalone Worker image did not contain `/app/engines/k6/target_apps_echo_smoke.js`.
- Worker image packaging now includes the safe demo k6 script, its local k6 helper, and the JMeter safe demo plan under `/app/engines/`.
- The safe demo profile still uses `engines/k6/target_apps_echo_smoke.js` and `engines/jmeter/target_apps_echo_latency_plan.jmx`; the Worker resolves those paths under `/app/`.
- The compose-oriented safe demo target is now `http://echo-api:8000`, so Worker and the local target service must share a Docker network where `echo-api` resolves.
- `target-apps/docker-compose.target-apps.yml` now supports a named default network through `PLOADTESTING_TARGET_APPS_NETWORK`; the smoke script uses a project-specific network name and cleans it up.
- Core still dispatches to the Worker through the registered IP address and port. Single-host compose should use shared networks; split-host deployment should set `WORKER_ADVERTISE_IP` to a Core-routable IP.

## Validation

- `git diff --check`
- `./.venv/bin/python -m pytest target-app/ target-apps/tests -v`
- `cd control-plane && ./.venv/bin/python manage.py check`
- `cd control-plane && ./.venv/bin/python manage.py test apps/ --verbosity=2`
- `cd control-plane && PLOADTESTING_ENABLE_DEMO_TASK_API=true ./.venv/bin/python manage.py test apps/ --verbosity=2`
- `./.venv/bin/python -m pytest workers/tests -v`
- `docker compose -f target-apps/docker-compose.target-apps.yml config --quiet`
- `bash target-apps/scripts/smoke_docker_target_apps.sh`
- `docker build -t local/ploadtesting-worker:0.1.0-rc.1 workers`
- `docker build -t local/ploadtesting-control-plane:0.1.0-rc.1 control-plane`
- Worker image script presence smoke for k6 script, k6 helper, and JMeter plan.

Image result:

- Worker image ID: `sha256:2b436572b46f3b9413cc6bdf4681e1636e18cf6097b756ba8e3f86612f7e50c7`
- Worker created: `2026-06-19T18:10:40.530664048Z`
- Worker size: `335277402`
- Worker CMD: `["python","agent.py"]`
- Control Plane image ID: `sha256:62cabdf236207752fe7d3163509662aae4ceefd3b183689bb6caf740b380fe1a`
- Control Plane created: `2026-06-19T18:10:40.533090549Z`
- Control Plane size: `79482618`
- Control Plane CMD: `["python","manage.py","runserver","0.0.0.0:8000"]`

Bounded integration smoke:

- Catalog and coverage endpoints: HTTP 200.
- Safe demo submit with `echo-api` / `echo-k6-smoke`: HTTP 201, initial status `dispatched`.
- Observed task status progression: `dispatched -> running -> completed`.
- Result summary: HTTP 200, `status=available`.
- Artifact metadata: HTTP 200, 5 rows.

## Follow-Ups

- Run the same safe demo task flow against the operator-managed external database and deployment compose pack before release candidate readiness review.
