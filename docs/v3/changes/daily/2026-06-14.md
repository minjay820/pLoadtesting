# 2026-06-14

## Scope

- Removed an unused InfluxDB import from the worker agent so the Worker lint job can pass `pyflakes`.
- Added explicit Control Plane diagnostics to the GitHub Actions CI workflow before Django tests run.
- Added an active runbook for CI Control Plane failure diagnosis.

## Changed Files

- `workers/agent.py`
- `.github/workflows/ci.yml`
- `docs/v3/runbooks/ci-control-plane-diagnostics.md`
- `docs/v3/runbooks/README.md`
- `docs/v3/changes/daily/2026-06-14.md`

## Summary

- Deleted the unused `WritePrecision` import from the optional InfluxDB client import block in `workers/agent.py`.
- Added a `Collect Control Plane diagnostics` step to CI that prints the Python version, pip version, installed package list, and `python manage.py check` output before `python manage.py test apps/ --verbosity=2`.
- Documented the new CI diagnostics flow in an active runbook and linked it from the runbooks index.

## Validation

- Not yet rerun in GitHub Actions after the workflow change.
- Local code review confirms the unused import is removed and the CI workflow remains syntactically valid.

## Commit

- Commit: Pending until commit is created.
