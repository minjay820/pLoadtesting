# pLoadtesting — InfluxDB v2 Initial Setup
# These values must match docker-compose.yml environment variables.
# Copy this file to .env or reference directly.

INFLUXDB_ADMIN_PASSWORD=ploadtesting-admin
INFLUXDB_TOKEN=ploadtesting-dev-token
INFLUXDB_ORG=ploadtesting
INFLUXDB_BUCKET=load_tests
