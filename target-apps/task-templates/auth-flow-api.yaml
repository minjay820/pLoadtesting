target_app_id: auth-flow-api
profiles:
  - target_profile_id: auth-k6-checkout
    display_name: Auth Checkout via k6
    description: Login, profile fetch, and checkout flow against auth-flow-api.
    engine: k6
    equivalent_profile_id: auth-jmeter-checkout
    script_path: engines/k6/target_apps_auth_checkout.js
    target_url: http://127.0.0.1:18086
    execution:
      duration_seconds: 20
      ramp_up_seconds: 0
      ramp_down_seconds: 0
      stop_policy: graceful_stop
      graceful_stop_seconds: 10
      max_run_seconds: 40
      iteration_limit:
      data_policy: duration_first
    parameters:
      TARGET_URL: http://127.0.0.1:18086
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      DEMO_SKU: sku-1
      DEMO_QUANTITY: "1"
  - target_profile_id: auth-jmeter-checkout
    display_name: Auth Checkout via JMeter
    description: JMeter login, profile, and checkout business flow against auth-flow-api.
    engine: jmeter
    equivalent_profile_id: auth-k6-checkout
    script_path: engines/jmeter/target_apps_auth_flow_plan.jmx
    target_url: http://127.0.0.1:18086
    execution:
      duration_seconds: 20
      ramp_up_seconds: 1
      ramp_down_seconds: 0
      stop_policy: graceful_stop
      graceful_stop_seconds: 10
      max_run_seconds: 40
      iteration_limit:
      data_policy: duration_first
    parameters:
      target_url: http://127.0.0.1:18086
      TARGET_HOST: 127.0.0.1
      TARGET_PORT: "18086"
      FLOW_MODE: checkout
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      DEMO_SKU: sku-1
      DEMO_QUANTITY: "1"
      ACCESS_TOKEN_USES: "2"
  - target_profile_id: auth-k6-refresh-flow
    display_name: Auth Refresh Flow via k6
    description: Bounded login, expiry, refresh, and logout flow against auth-flow-api.
    engine: k6
    equivalent_profile_id: auth-jmeter-refresh-flow
    script_path: engines/k6/target_apps_auth_refresh_flow.js
    target_url: http://127.0.0.1:18086
    parameters:
      TARGET_URL: http://127.0.0.1:18086
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      ACCESS_TOKEN_USES: "1"
      REFRESH_USES: "2"
  - target_profile_id: auth-k6-failure-branches
    display_name: Auth Failure Branches via k6
    description: Invalid credential, expired token, and revoked token checks against auth-flow-api.
    engine: k6
    equivalent_profile_id: auth-jmeter-failure-branches
    script_path: engines/k6/target_apps_auth_refresh_flow.js
    target_url: http://127.0.0.1:18086
    parameters:
      TARGET_URL: http://127.0.0.1:18086
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      ACCESS_TOKEN_USES: "1"
      REFRESH_USES: "1"
      ASSERT_FAILURE_BRANCHES: "1"
  - target_profile_id: auth-jmeter-failure-branches
    display_name: Auth Failure Branches via JMeter
    description: JMeter invalid credential, expiry, refresh, and revoked token branch validation.
    engine: jmeter
    equivalent_profile_id: auth-k6-failure-branches
    script_path: engines/jmeter/target_apps_auth_flow_plan.jmx
    target_url: http://127.0.0.1:18086
    parameters:
      target_url: http://127.0.0.1:18086
      TARGET_HOST: 127.0.0.1
      TARGET_PORT: "18086"
      FLOW_MODE: failure-branches
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      ACCESS_TOKEN_USES: "1"
      REFRESH_USES: "1"
  - target_profile_id: auth-k6-session-flow
    display_name: Auth Session Flow via k6
    description: Demo-only cookie login, session profile, and session logout flow against auth-flow-api.
    engine: k6
    equivalent_profile_id: auth-jmeter-session-flow
    script_path: engines/k6/target_apps_auth_session_mfa_flow.js
    target_url: http://127.0.0.1:18086
    parameters:
      TARGET_URL: http://127.0.0.1:18086
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      FLOW_MODE: session
      SESSION_USES: "2"
  - target_profile_id: auth-k6-mfa-flow
    display_name: Auth MFA Flow via k6
    description: Deterministic MFA-like challenge and verify flow with bearer issuance against auth-flow-api.
    engine: k6
    equivalent_profile_id: auth-jmeter-mfa-flow
    script_path: engines/k6/target_apps_auth_session_mfa_flow.js
    target_url: http://127.0.0.1:18086
    parameters:
      TARGET_URL: http://127.0.0.1:18086
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      FLOW_MODE: mfa
      MFA_CHANNEL: sms
      MFA_ISSUE_MODE: bearer
  - target_profile_id: auth-jmeter-refresh-flow
    display_name: Auth Refresh Flow via JMeter
    description: JMeter bearer expiry, refresh, and logout flow against auth-flow-api.
    engine: jmeter
    equivalent_profile_id: auth-k6-refresh-flow
    script_path: engines/jmeter/target_apps_auth_flow_plan.jmx
    target_url: http://127.0.0.1:18086
    parameters:
      target_url: http://127.0.0.1:18086
      TARGET_HOST: 127.0.0.1
      TARGET_PORT: "18086"
      FLOW_MODE: refresh
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      ACCESS_TOKEN_USES: "1"
      REFRESH_USES: "2"
  - target_profile_id: auth-jmeter-session-flow
    display_name: Auth Session Flow via JMeter
    description: JMeter demo-only cookie session flow against auth-flow-api.
    engine: jmeter
    equivalent_profile_id: auth-k6-session-flow
    script_path: engines/jmeter/target_apps_auth_flow_plan.jmx
    target_url: http://127.0.0.1:18086
    parameters:
      target_url: http://127.0.0.1:18086
      TARGET_HOST: 127.0.0.1
      TARGET_PORT: "18086"
      FLOW_MODE: session
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      SESSION_USES: "2"
  - target_profile_id: auth-jmeter-mfa-flow
    display_name: Auth MFA Flow via JMeter
    description: JMeter deterministic MFA-like challenge and verify flow against auth-flow-api.
    engine: jmeter
    equivalent_profile_id: auth-k6-mfa-flow
    script_path: engines/jmeter/target_apps_auth_flow_plan.jmx
    target_url: http://127.0.0.1:18086
    parameters:
      target_url: http://127.0.0.1:18086
      TARGET_HOST: 127.0.0.1
      TARGET_PORT: "18086"
      FLOW_MODE: mfa
      DEMO_USERNAME: alice
      DEMO_PASSWORD: demo-password
      MFA_CHANNEL: sms
      MFA_ISSUE_MODE: bearer
